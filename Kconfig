#
# Copyright (c) 2022 Nordic Semiconductor
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#
mainmenu "Matter nRF Connect Template Example Application"

config SIMULATED_PIR_SENSOR
	bool "Simulated PIR sensor with button"
	help
	  Enable this option to simulate PIR sensor input using a button.

config HOLD_TIME_LIMIT_MIN_SEC
	int "PIR sensor hold time limit (min)"
	default 1
	range 1 600
	help
	  Minimum time in seconds that the occupancy state remains "occupied"
	  after motion is detected.

config HOLD_TIME_LIMIT_MAX_SEC
	int "PIR sensor hold time limit (max)"
	default 600
	range 1 600
	help
	  Maximum time in seconds that the occupancy state remains "occupied"
	  after motion is detected.

config HOLD_TIME_LIMIT_DEFAULT_SEC
	int "PIR sensor hold time limit (default)"
	default 10
	range HOLD_TIME_LIMIT_MIN_SEC HOLD_TIME_LIMIT_MAX_SEC
	help
	  Default time in seconds that the occupancy state remains "occupied"
	  after motion is detected.

config RFS_DEVICE1_MAC_ADDR
	hex "RF Sensing Device 1 MAC Address"
	default 0xAABBCCDDEEFF
	help
	  MAC address of RF Sensing Device 1 (6 bytes as hex).
	  Device 1 will use Matter endpoint 2 for occupancy sensing.
	  Format: 0xAABBCCDDEEFF where AA is MSB, FF is LSB.

config RFS_DEVICE2_MAC_ADDR
	hex "RF Sensing Device 2 MAC Address" 
	default 0xAABBCCDDEEFF
	help
	  MAC address of RF Sensing Device 2 (6 bytes as hex).
	  Device 2 will use Matter endpoint 3 for occupancy sensing.
	  Format: 0xAABBCCDDEEFF where AA is MSB, FF is LSB.

# Sample configuration used for Thread networking
if OPENTHREAD

choice OPENTHREAD_NORDIC_LIBRARY_CONFIGURATION
	default OPENTHREAD_NORDIC_LIBRARY_MTD
endchoice

choice OPENTHREAD_DEVICE_TYPE
	default OPENTHREAD_MTD
endchoice

config CHIP_ENABLE_ICD_SUPPORT
	default y

# Reduce Thread TX output power to 0 dBm for SED device
config OPENTHREAD_DEFAULT_TX_POWER
	int
	default 0

endif # OPENTHREAD

source "${ZEPHYR_CONNECTEDHOMEIP_MODULE_DIR}/config/nrfconnect/chip-module/Kconfig.features"
source "${ZEPHYR_CONNECTEDHOMEIP_MODULE_DIR}/config/nrfconnect/chip-module/Kconfig.defaults"
source "${ZEPHYR_NRF_MODULE_DIR}/samples/matter/common/src/Kconfig"
source "Kconfig.zephyr"
